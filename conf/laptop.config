// Laptop / local CPU profile
// Use Docker for reproducibility when available; fall back to local for the scaffold smoke test

process {
    executor = 'local'
    cpus     = 2
    memory   = '4 GB'
}

docker {
    enabled = false   // flip to true once Phase 1 container exists
    runOptions = '-u $(id -u):$(id -g)'
}
