// GPU profile for Phase 2 mutant-structure prediction (Boltz-1, Chai-1)

process {
    withLabel:gpu {
        accelerator = [request: 1, type: 'a100']
        memory      = '32 GB'
        cpus        = 4
    }
}

docker {
    enabled = true
    runOptions = '--gpus all -u $(id -u):$(id -g)'
}
